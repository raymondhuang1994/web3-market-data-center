# 晨间启动保障与恢复边界

2026-09-16：GitHub 四个 schedule 在创建 run 前延迟约四个半至五小时；Mac 在任务生成后 5–14 秒接手。不能用安装成功证明准点运行。今日 14:40 香港时间发布，只复用原有截止前数据，新增来源未完成晨间验收。

## 两层启动

- GitHub 原排程 08:37、08:52、09:07、09:32 保留；Mac 官方运行器继续执行，OIDC 身份验证不变。
- Codex 本机补充检查 08:30、08:50，使用现有 GitHub 连接，按下列规则更新同一天唯一的 data/requests/YYYY-MM-DD.json。只写 reportDate 和 requestedAt，后者为实际当前 UTC 时间。无需新增 API 密钥或插件。
- 同一项 Codex 定时任务包含 08:30、08:50、09:10、09:35、10:05 五个检查点；后面三轮核查成版及是否超过10点目标。只有同版数据、AI 与 PDF 完整发布才算发布成功。晨间采样与发布成功分别记录。
- 每天包括周末及香港节假日；Mac 和 Codex 需在线。自动唤醒仍待管理员认证，保活不能唤醒已睡眠机器。10:00 是目标，不承诺硬性 SLA。

## 截止前检查

先读 GitHub 当日运行及任务步骤、当日 edition 和对应 snapshot 数据。只在香港 08:25–08:55 执行启动保障；晚到的检查按截止后规则处理，不倒填。

1. 有 queued/in_progress/waiting/pending/requested 任务时，不重复提交。
2. 08:30 检查以 08:25 为本轮起点，08:50 检查以 08:45 为起点。本轮起点之后已有成功 collect+stage 且实际 fetchedAt 属于今日并早于09:00的记录，则本轮无需再触发。generatedAt 只是组装时间，不能当作采样证据。
3. 否则，读取当日 request 文件及 blob SHA；文件不存在则创建，存在则以当前 SHA 更新。若 requestedAt 已属于本轮，检查对应 push run，不能再次提交同一轮请求。写入前再查活动任务，减少与原排程的重复。
4. 临时错误只允许对同一个 run 重跑一次；利用 run_attempt 和本地 work/monitor 记录避免跨核查重复重试。账户、权限、额度阻断不重跑。
5. 原排程与保障请求偶尔重叠时，由现有 workflow concurrency 串行执行，不取消任务。

## 截止后恢复

工作流在09:00后进行阶段选择时，将迟到的 collect 转为 finalize；若阶段选择与采集恰好跨越09:00，采集器仍执行已有截止过滤及存量回退，不会抓取晚到实时行情。优先读取当天已选定或冻结版本；已发布就退出。不存在草稿时才复用截止前存量，实际来源日期保持不变。不得主动采集晚到实时行情，亦不重新生成已经冻结的分析。

未发布且无活动任务时，先查今天的 finalize：完全没有 finalize 才更新唯一 request 触发一次；有临时失败且 run_attempt=1 才重跑原 run 一次；attempt>=2 不再重跑或用新请求绕过上限。遇账户/权限/额度错误保留旧版并反馈。状态无变化保持安静。

## 模型恢复

唯一模型仍为免费 glm-4.7-flash。仅从有限长度错误体提取官方白名单业务码；不保留原始消息、密钥或账号内容。明确区分短期限流、平台过载、额度及权限问题。未知429不推断具体原因。

每项工作流最多四次模型调用，指数退避并尊重 Retry-After；预计等待加一次请求超过十分钟预算就结束，不截短服务端等待。记录安全诊断到私有工作流日志。账户/额度阻断不自动重试，不升级付费模型。已冻结解读和原子发布规则不变。

错误码依据：https://docs.bigmodel.cn/cn/api/api-code
